import React from "react";
import ReactDOM from "react-dom/client";
import BookingSystem from "./book.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BookingSystem />
  </React.StrictMode>
);
